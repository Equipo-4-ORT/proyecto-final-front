VITE_API_URL=http://localhost:3000

# En desarrollo con docker compose, el bundle del front corre
# en el navegador del developer (fuera de Docker), así que tiene
# que apuntar al puerto del back EXPUESTO EN EL HOST, no al
# hostname interno de la red de Compose ("app" no se resuelve
# desde el navegador).
